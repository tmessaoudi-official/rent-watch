{
    "name": "tmessaoudi/scout",
    "description": "scout — one self-hosted watcher, several domains: rental listings (rent) and used cars (car) in Île-de-France",
    "type": "project",
    "license": "proprietary",
    "require": {
        "php": ">=8.5",
        "ext-curl": "*",
        "ext-dom": "*",
        "ext-json": "*",
        "ext-mbstring": "*",
        "ext-openssl": "*",
        "ext-pdo_sqlite": "*"
    },
    "autoload": {
        "psr-4": {
            "Scout\\": "src/php/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Scout\\Tests\\": "tests/php/"
        }
    },
    "config": {
        "sort-packages": true,
        "optimize-autoloader": true
    },
    "scripts": {
        "test": "php tools/phpunit.phar"
    },
    "suggest": {
        "ext-pcntl": "Clean shutdown of `scout run --watch`: without it a SIGTERM kills the process mid-pass instead of after it, which is how a redeploy produces duplicate notifications. WatchLoop degrades gracefully and says so on its banner."
    }
}
